export interface Register {
        userId?: number;
        firstName: string;
        lastName: string;
        email: string;
        passWord: string;
        Confirmpassword?: string;
        dept: string;
        position: string;
        hiredate?: string; // Adjust format as needed
        birthdate?: string; // Adjust format as needed
        address: string;
      }

